# Свод ФОТ и страховых взносов (соцтакси, 2024–2025)

Рабочая память сессии, цифры и открытые задачи: **[docs/Протокол рабочей сессии v2.md](docs/Протокол%20рабочей%20сессии%20v2.md)** (актуально, **k = 0,63**). Архив v1: [docs/Протокол рабочей сессии.md](docs/Протокол%20рабочей%20сессии.md).

Набор скриптов помогает собрать годовую сводную таблицу по сотрудникам:

- ФОТ за **2024** год из файла `Svodnyi-po-ZP-za-2024-god.xlsx` → `python svod_2024.py` → `Personal_2024_soc_taxi.xlsx`;
- ФОТ за **2025** год из `Svodnyi-po-ZP-za-2025-god.xlsx` → `python svod_2025.py` → `Personal_2025_soc_taxi.xlsx` (общая логика в `svod_personal_core.py`);
- страховые взносы из файлов `Nalogi-i-vznosy*.xlsx` (для 2025 при наличии предпочтительны имена с **`2025`** в названии, иначе — все `Nalogi-i-vznosy*.xlsx` или переменная **`VZNOSY_GLOB`**);
- итоговые таблицы `Itog_personal_vznosy_{год}.xlsx` и целевые `Personal_{год}_soc_taxi.xlsx` с долями участия в соцтакси.

## Подготовка

Положите в эту папку исходные Excel-файлы:

```text
Svodnyi-po-ZP-za-2024-god.xlsx
Svodnyi-po-ZP-za-2025-god.xlsx
Nalogi-i-vznosy-1.xlsx
Nalogi-i-vznosy-2.xlsx
...
```

Файлы по взносам могут быть помесячными. Имена должны начинаться с `Nalogi-i-vznosy`. Для **2025** при нескольких годах в папке задайте маску: `set VZNOSY_GLOB=Nalogi-i-vznosy*2025*.xlsx` (Windows) перед `python svod_2025.py`.

Для **полного документального комплекта** (пояснительная записка из одного репозитория): положите PDF в **`docs/dogovory/`** по [каноническому списку имён](docs/dogovory/README.md); связка с группами расходов — в разделе **«Документальное обоснование»** ниже.

## Установка зависимостей

Откройте командную строку в папке проекта:

```bash
cd C:\taganay_svod_2024
pip install -r requirements.txt
```

## Запуск

Основной сценарий запускается одной командой:

```bash
python svod_2024.py
# при наличии исходников за 2025:
python svod_2025.py
```

После успешного запуска за **2024** будут созданы файлы:

```text
Personal_2024_auto.xlsx
Vznosy_2024_auto.xlsx
Itog_personal_vznosy_2024.xlsx
Personal_2024_soc_taxi.xlsx
```

После `python svod_2025.py` — аналогично с суффиксом **`_2025`** и листами `Персонал_2025_soc_taxi`, `Группа_затрат_2025`.

Файл `Personal_2024_soc_taxi.xlsx` содержит:

- ФИО;
- категорию участия;
- долю участия в соцтакси;
- ФОТ за 2024 год;
- страховые взносы за 2024 год;
- итог ФОТ + взносы;
- сумму, отнесённую на соцтакси.
- лист `Группа_затрат_2024` с агрегированной таблицей для пояснительной записки.

Сотрудники, которые не входят в матрицу участия в соцтакси, автоматически помечаются как `Другая деятельность (Роснефть/прочее)` с долей участия `0`.

Группы затрат формируются автоматически:

- `1. ФОТ водителей и соцработников`;
- `2. ФОТ диспетчера`;
- `3. ФОТ ремонтного персонала (Егоров, Шакалов, Родионов)`;
- `4. ФОТ руководителя соцслужбы (Лопакова Н.Ф.)`;
- `5. ФОТ руководителя организации (50% Богдановского)`;
- `6. ФОТ бухгалтерии, IT и админ-персонала`;
- `0%. Другая деятельность (Роснефть/прочее)`.

## Как воспроизвести расчёт тарифа за 2024 год с нуля

Один и тот же порядок можно повторить на чистой копии проекта, чтобы показать **проверяемость и воспроизводимость** расчёта (ФОТ, прочие расходы по 60-й, сверка с 76-й).

Предполагается, что выполнены разделы **«Подготовка»** и **«Установка зависимостей»**, рабочая папка — `C:\taganay_svod_2024`.

| Шаг | Действие |
|-----|----------|
| 1 | Распаковать архив с ОСВ (например `ОСВ и свод.zip`) в `_extract_osv`. |
| 2 | `python scripts/rename_extract_osv_files.py` — появятся `Osv_schet_60_2024.xls`, `Osv_schet_76_2024.xls` и др. |
| 3 | `python svod_2024.py` — при необходимости `python svod_2025.py` (нужны `Svodnyi-po-ZP-za-2025-god.xlsx` и взносы за 2025). |
| 4 | `python svod_osv60.py --year 2024` → `Osv60_soc_taxi_2024.xlsx`. Для 2025: `python svod_osv60.py --year 2025`. |
| 5 | `python svod_reconcile_osv76.py --year 2024` → `Osv76_sverka_2024.xlsx` (свой файл: `--osv "…\файл.xlsx"`). |
| 6 | `python svod_osv60.py --year 2025` и `python svod_reconcile_osv76.py --year 2025` — при наличии ОСВ за 2025. |
| 7 | `python compare_osv60.py` → `Svod_60_2024_2025.xlsx` (сравнение строк записки по счёту 60). |

**Правила по счёту 60:** в сырой выгрузке 1С обычно **нет** колонки «Статья расходов» — тогда классификация только по **контрагенту** (`svod_osv60_core.py`). Если добавили столбец со статьёй: `python svod_osv60.py --year 2024 --article-col 7` (0-based; колонка H = 7). Ключевые слова по статье — `config_osv60.py`. Исходник по умолчанию ищется в `_extract_osv`: предпочтительно **`Osv_schet_60_{год}.xls`**; при нескольких файлах **`.xls` важнее `.xlsx`**, архивные `*_old*` — ниже приоритетом; файл с расширением `.xls`, но с содержимым как у **.xlsx** (ZIP), читается автоматически. Не кладите **готовый свод** под именем `Osv_schet_60_{год}.xls` — если в A1 стоит «Строка», скрипт выберет другую сырую выгрузку или сообщит об ошибке.

## Воспроизводимый pipeline (2024 / 2025)

Одинаковая логика для двух лет: сначала переименование ОСВ, затем ФОТ, затем счёт 60 и сверка 76.

```bash
cd C:\taganay_svod_2024
pip install -r requirements.txt
python scripts/rename_extract_osv_files.py
python svod_2024.py
python svod_2025.py
python svod_osv60.py --year 2024
python svod_reconcile_osv76.py --year 2024
python svod_osv60.py --year 2025
python svod_reconcile_osv76.py --year 2025
python compare_osv60.py
```

**`svod_2025.py`:** см. `python svod_2025.py -h`. После расчёта в консоли — **ИТОГО** по листу `Группа_затрат_2025`; сверка со строкой счёта 70 в `Svod-2024-2025.xlsx` (ориентир **~3 629 342,14** руб. из учёта; при полном совпадении методики с **k = 0,63** суммы должны сходиться после перепроведения распределения в 1С).

**Договоры в `docs/dogovory/`:** структура и таблица — [docs/dogovory/README.md](docs/dogovory/README.md); массовое копирование PDF: `python scripts/sync_dogovory.py` (переменная **`DOGOVORY_SOURCE`**).

**Где исходники:** сводные ЗП — `Svodnyi-po-ZP-za-{год}-god.xlsx` в **корне**; взносы — `Nalogi-i-vznosy*.xlsx`; выгрузки ОСВ из 1С — в **`_extract_osv`** (после `rename_extract_osv_files.py` — имена вида `Osv_schet_60_2024.xls`).

**Что на выходе (основное):**

| Файл | Назначение |
|------|------------|
| `Personal_2024_soc_taxi.xlsx` | ФОТ+взносы 2024, доли соцтакси; листы `Персонал_2024_soc_taxi`, `Группа_затрат_2024` |
| `Personal_2025_soc_taxi.xlsx` | То же за **2025** (`svod_2025.py`), листы `Персонал_2025_soc_taxi`, `Группа_затрат_2025` |
| `Osv60_soc_taxi_{год}.xlsx` | Прочие расходы по 60: см. листы ниже |
| `Osv76_sverka_{год}.xlsx` | **Сверка** ОСВ 76 с персоналом (не входит в себестоимость; см. шапку `svod_reconcile_osv76.py`) |
| `Svod_60_2024_2025.xlsx` | Сравнение групп записки 2024 vs 2025 (`compare_osv60`) — **готово к приложению** к пояснительной по счёту 60 |
| `Svod_2024_2025_iz_arhiva_OSV_i_svod.xlsx` и **`Svod-2024-2025.xlsx`** | Один файл: копия сводного баланса из отчётов (`sync_svod_2024_2025_from_otchety.py`); второе имя — как в [протоколе v2](docs/Протокол%20рабочей%20сессии%20v2.md) |

**Листы `Osv60_soc_taxi_{год}.xlsx`:**

| Лист | Содержание |
|------|------------|
| `Строки_ОСВ60` | Контрагент, обороты, статья (если была в файле), группа, сумма на соцтакси |
| `Свод_по_группам` | Агрегат по внутренней группе: сумма кредита оборота и сумма на соцтакси |
| `Не_включено_в_свод` | Строки без правила классификации / банки |
| `Группа_затрат_записка` | 7 строк + итог для вставки в пояснительную записку |

Скрипт **`svod_osv60.py`** внутри строит отчёт через функции `load_osv60` → `build_svod` → таблица записки → `save_report` (см. модуль `svod_osv60.py`). Правила по статьям — `config_osv60.py`, по контрагентам — `svod_osv60_core.py`. **Доля косвенных (k)** по годам — `config_allocation.py`; для админ-долей ФОТ используется **`get_soc_taxi_share(год)`** в [`svod_personal_core.py`](svod_personal_core.py).

## Документальное обоснование

Пояснительная записка для суда воспроизводима из одного репозитория, если в каталоге **`docs/dogovory/`** лежат PDF по [каноническому списку](docs/dogovory/README.md). Ниже — связка **строк листа `Группа_затрат_записка`** (`svod_osv60_core.py`) с документами; машинный расчёт — в **`Osv60_soc_taxi_{год}.xlsx`**.

### Прочие расходы по счёту 60 (7 строк записки)

| № записки | Группа в пояснительной записке | Внутренние группы (`config_osv60.py`) | Документы (PDF в репозитории) |
|-----------|-------------------------------|----------------------------------------|------------------------------|
| 1 | Аренда специализированного транспортного средства и гаража | `Аренда автомобиля`, `Аренда гаража/стоянки` | [Договор аренды VW + допсоглашение](docs/dogovory/dogovor-arenda-volkswagen-lopakova-dopsoglashenie.pdf); при необходимости обоснования парка — [КП ГАЗель NEXT](docs/dogovory/kp-gazel-next.pdf) |
| 2 | Расходы на топливо (ГСМ) | `Топливо (ГСМ)` | Первичка по контрагентам сверяется с листом `Строки_ОСВ60`; при наличии — договоры с поставщиками ГСМ в `docs/dogovory/` (произвольное имя, см. [dogovory/README](docs/dogovory/README.md)) |
| 3 | Текущий ремонт, обслуживание и автотовары | `Ремонт и обслуживание автомобиля` | Договоры со СТО / накладные (по мере добавления в `docs/dogovory/`) |
| 4 | Страхование транспортных средств | `Страхование автомобиля` | Полисы ОСАГО и иные (по мере добавления в `docs/dogovory/`) |
| 5 | Медицинские осмотры водителей | `Медосмотр водителей` | Договор с медорганизацией (по мере добавления в `docs/dogovory/`) |
| 6 | Мойка автомобилей и санитарное состояние | `Мойка автомобилей` | Договор с мойкой (по мере добавления в `docs/dogovory/`) |
| 7 | Прочие эксплуатационные (косвенные, × доля выручки по соцуслугам) | `Коммунальные услуги`, `Связь, IT и офисные расходы`, `Прочие эксплуатационные` | [ОСВ 60 за 2024 (PDF)](docs/dogovory/osv-60-2024.pdf) — согласование итогов учёта; [сводный отчёт по поездкам](docs/dogovory/svodnyi-otchet-poezdki-sottaksi.pdf) — доля **63%** (оба года — см. `config_allocation.py`, протокол v2); договоры на связь, аренду помещений и т.д. — по мере добавления |

Сводная **выгрузка 1С** по счёту 60 в формате Excel остаётся в **`_extract_osv/Osv_schet_60_{год}.xls`** — она участвует в скриптах; PDF в `docs/dogovory/` дублирует учёт для приложения к материалам суда.

### Банковские выписки (подтверждение платежей)

Копии выписок **Сбер** и **ВТБ** за **2024** и **2025** лежат в **`docs/bank_vypiski/`** ([описание](docs/bank_vypiski/README.md)). Обновить с диска `E:\`: `python scripts/sync_bank_vypiski.py`.

### ФОТ и страховые взносы (лист `Группа_затрат_2024`)

| Блок | Документы |
|------|-----------|
| Свод по начисленной ЗП | [Svodnyi-po-ZP-za-2024-god.pdf](Svodnyi-po-ZP-za-2024-god.pdf) (если файл есть в корне репозитория) |
| Группы ФОТ в записке | Трудовые договоры, штатное расписание, приказы — рекомендуется складывать в `docs/dogovory/` с префиксом `trudovoj-` / `prikaz-` и кратко перечислять в коммите или в [dogovory/README](docs/dogovory/README.md) |

Публичный репозиторий для совместной работы: [kraskimira89-spec/taganay_svod_2024](https://github.com/kraskimira89-spec/taganay_svod_2024) (ветка `main`).

## Если скрипт не находит колонки

Запустите вспомогательный скрипт:

```bash
python debug_svod.py
```

Он покажет реальные названия листов и колонок в файле `Svodnyi-po-ZP-za-2024-god.xlsx`. После этого при необходимости поправьте ожидаемые имена колонок в `svod_personal_core.py` (или исходный Excel).

## Контрольные скрипты (протокол)

- `python scripts/verify_arenda_lopakova_osv60.py` — сумма «На_соцтакси» по **Лопакова** в `Osv60_soc_taxi_2024.xlsx` vs **1 080 000** руб./год (договор аренды VW).
- `python scripts/check_vznosy_rate_2024.py` — совокупная доля взносов к базе по каждому файлу `Nalogi-i-vznosy*.xlsx` (ориентир **7,2%**).
- `python scripts/check_amort_protocol_numbers.py` — напоминание цифр Кт по счёту **02** из протокола и наличие `Osv_schet_02_{год}.xls`.
- `python scripts/export_zayavki_gku_cst.py --year 2025` — реестр **«Заявки … ГКУ ЦСТ»** в `data/gku_cst/{год}/`, свод `zayavki_*_svod.xlsx` и CSV; связь с PDF «сводный отчёт» — в `docs/sverki/Zayavki_GKU_CST.md`.

## ОСВ по счёту 60 (прочие расходы для записки)

Кратко: распакуйте выгрузки в `_extract_osv`, при необходимости `python scripts/rename_extract_osv_files.py`, затем универсальная команда:

```bash
python svod_osv60.py --year 2024
python svod_osv60.py --year 2025
```

Полный порядок с ФОТ и сверкой 76 — в разделе **«Воспроизводимый pipeline»** и **«Как воспроизвести расчёт тарифа за 2024 год с нуля»**.

Параметры: `--osv-path`, `--output`, `--article-col` (см. `python svod_osv60.py -h`).

**«Верная» ОСВ из бухгалтерии** (12 колонок, статья расходов справа от сумм) — столбец статьи **определяется автоматически** (обычно индекс 7). Пример с вашим путём:

```bash
python svod_osv60.py --year 2024 --osv-path "E:\ЦИП Таганай\6-БУХГАЛТЕРИЯ\Оборотно-сальдовые ведомости\Оборотно-сальдовая ведомость верная по счету 60 за 2024 г.xls"
```

Чтобы подтянуть «верные» файлы из бухгалтерии в `_extract_osv` без длинных `--osv-path`:

- **60 за 2024:** `python scripts/sync_osv60_vernaya_2024.py` (`OSV60_VERNAYA_2024` при необходимости).
- **60 за 2025:** `python scripts/sync_osv60_vernaya_2025.py` (`OSV60_VERNAYA_2025`); исходник по умолчанию — `ОБС верно по счету 60 за 2025 г.xls`.
- **Пакет из папки бухгалтерии** (`E:\ЦИП Таганай\6-БУХГАЛТЕРИЯ\…`): `python scripts/sync_extract_osv_from_buhgalteriya.py` (переменная **`BUHGALTERIYA_OSV_DIR`**).
- **70 за 2024:** `python scripts/sync_osv70_vernaya_2024.py` (`OSV70_VERNAYA_2024`).
- **70 за 2025** (архив в проекте, свода по 70 пока нет): `python scripts/sync_osv70_vernaya_2025.py` (`OSV70_VERNAYA_2025`); копия как `Osv_schet_70_2025.xls`.
- **01 и 02 (ОС и амортизация):** задайте `OSV01_2024`, `OSV02_2024`, `OSV01_2025`, `OSV02_2025` и выполните `python scripts/sync_osv_01_02.py`. Контроль цифр из протокола: `python scripts/check_amort_protocol_numbers.py`.
- **Банковские выписки** (Сбер, ВТБ, 2024–2025): `python scripts/sync_bank_vypiski.py` → `docs/bank_vypiski/` (`BANK_VYPISKI_*` при необходимости).

Результаты: `Osv60_soc_taxi_2024.xlsx` / `Osv60_soc_taxi_2025.xlsx` — см. раздел **«Листы Osv60_soc_taxi»** выше. В `_extract_osv` может лежать свод финпоказателей за год: **`Svod_finansovye_pokazateli_2024.xlsx`** или **`Svod_finansovye_pokazateli_2024.xls`** (классический BIFF из 1С — не путать с ОСВ по счёту 60).

**Сравнение 2024 и 2025:** `python compare_osv60.py` → `Svod_60_2024_2025.xlsx`.

Копия свода из папки отчётов (`ОСВ и свод/Свод 2024-2025 .xlsx`): `python scripts/sync_svod_2024_2025_from_otchety.py` → в корне **`Svod_2024_2025_iz_arhiva_OSV_i_svod.xlsx`** и дубликат **`Svod-2024-2025.xlsx`** (имя как в протоколе; содержимое одинаковое). Машинное сравнение групп по счёту 60 — это отдельный файл **`Svod_60_2024_2025.xlsx`** из `compare_osv60.py`.

В свод попадают только строки с известным правилом (остальное — на лист исключений). Топливом по контрагенту считаются **Ликард** и **Газпромнефть** (АЗС); прочие «Газпром*» — **коммунальные**; на косвенные расходы применяется доля приходов по соцуслугам из **`config_allocation.py`** (**2024 и 2025:** **0,63** — не из счёта 62; см. `docs/Протокол рабочей сессии v2.md`). Правки по статьям — `config_osv60.py`, по контрагентам — `svod_osv60_core.py`.

## Сверка ОСВ по счёту 76 с персоналом

Счёт **76** в отчёте используется **только для сверки** с реестром ФЛ (нет задвоения с ФОТ по 70). См. описание в `svod_reconcile_osv76.py`.

После `rename_extract_osv_files.py` и файла **`Personal_{год}_soc_taxi.xlsx`**:

```bash
python svod_reconcile_osv76.py --year 2024
python svod_reconcile_osv76.py --year 2025
```

По умолчанию: **`Personal_{год}_soc_taxi.xlsx`**, лист **`Персонал_{год}_soc_taxi`**. Для произвольного файла: `--personal`, `--sheet-personal`. Свой Excel с колонкой «роль»: `--osv "…\файл.xlsx" --sheet-osv Лист1`.

## Пакет для Perplexity / внешнего ИИ

Один архив с исходниками, результатами расчётов и кодом (для составления отчётов и пояснительной записки):

```bash
python scripts/build_perplexity_bundle.py
```

В корне появится **`Perplexity_paket_Taganay_2024_2025.zip`** (внутри папка `taganay_perplexity_2024_2025/` и **`MANIFEST.txt`** со списком файлов).

## Старые скрипты

- `svod_personal.py` — отдельная сборка ФОТ.
- `svod_vznosy.py` — отдельная сборка взносов по одному файлу.
- `debug_svod.py` — диагностика колонок.

Для обычной работы используйте `svod_2024.py`.
